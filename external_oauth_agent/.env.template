# This file is used as a template for local development and testing.
# Copy this file to .env and replace the placeholder values with your actual credentials.

# Use Vertex AI instead of Google AI Studio
GOOGLE_GENAI_USE_VERTEXAI=1

# Replace with your actual Google Cloud Project ID and Location
GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# --- ADK Agent Deployment Variables ---
# The dynamic key Gemini Enterprise will use to pass the token (matches your registration)
AUTH_ID="my-adk-agent-auth"

# The target URI of your protected Cloud Run API (Update after Step 3)
API_URL="https://your-cloud-run-api-url.a.run.app/api/v1/protected-data"